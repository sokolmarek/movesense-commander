{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "// note": "baseUrl/paths are duplicated here because the shadcn CLI reads alias config from the root tsconfig.json and cannot see project references.",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  }
}
